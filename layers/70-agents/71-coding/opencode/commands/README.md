# OpenCode Commands Directory

This directory contains custom `/commands` that you can type directly into the OpenCode CLI.

## How to add commands:
Create markdown files where the filename is the slash command.

Example `commit.md`:
```markdown
# Commit Command
Create a highly detailed git commit with proper message formatting based on the git diff.
Usage: /commit
```
